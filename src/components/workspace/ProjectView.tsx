import Link from 'next/link';
import type {ProjectSubmission} from '@/lib/workspace/types';
export default function ProjectView({project,username,name}:{project:ProjectSubmission;username:string;name:string}){return <article className="ws-panel-stack"><Link href={`/u/${username}`}>← {name}’s workspace</Link><span className="eyebrow">{project.type.replaceAll('-',' ')}</span><h1>{project.title}</h1><p style={{whiteSpace:'pre-wrap'}}>{project.summary}</p><p className="ws-small">{project.status==='reviewed'?'Reviewed':'Learner-submitted work'} · Updated {project.updatedAt.slice(0,10)}</p>{project.url&&<a href={project.url} target="_blank" rel="noopener noreferrer" className="btn btn-primary">Explore the project ↗</a>}</article>;}
